Steps to follow

1. Run the command "npm i" to install the dependencies
2. Run the command "npm run start" to build and start the application in one go
3. Browse the url, which is shown in the terminal Eg:- http://localhost:4200/